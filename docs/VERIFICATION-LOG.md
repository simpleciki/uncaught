# Verification log

Every output IBM Bob produces is checked by an independent script before it is committed. The check reads files from disk and does not rely on Bob's summary. This log records what those checks found.

| # | Date (CDT) | Bob task | What Bob reported | What the independent check found | Resolution | Commit |
|---|---|---|---|---|---|---|
| 1 | 2026-09-25 | Task 01, Plan mode | A plan with 10 failure patterns and exact line references | All 10 line references match `subject/quick-lru/index.js`. Review found 5 design gaps: no baseline gate, test dependencies unresolvable from a system temp folder, 3 operator flips not labelled as such, a "check Bob's tests" step that re-used the same mutants (it could only pass), and a `file://` site that browsers block | Bob rewrote the plan with all 5 corrections (`docs/PLAN.md`) | `0ab81d1` |
| 2 | 2026-09-25 | Task 02, custom modes | Saboteur and Guardian modes created | The Saboteur write rule did not include `patterns/`, so the mode could not write the catalog it was built to write | Bob fixed the rule | `3afad8f` |
| 3 | 2026-09-25 | Task 03, Saboteur mode | "10 + 10 + 10. All files written", with a clean summary table | **7 of 20 mutant files were not valid JSON.** Multi-line `find` strings contained raw tab and newline characters. The runner would have crashed on the first of them | Bob escaped the 7 files. Re-check: 19 valid, 1 honestly skipped, 0 invalid. Every `find` matches the source exactly once, and no mutant shares a `find` with another | `9cf81b9` |
| 4 | 2026-09-25 | Task 04, Agent mode | Runner reported FP-03, FP-04, FP-05, FP-09, FP-10 as "caught" | **False catches caused by a timing-sensitive test.** Running the unmutated subject 4 times in parallel showed "expiresIn() returns remaining ms for expiring item" fails 3/4 times under CPU load; it asserts an exact millisecond value. FP-05 was confirmed by hand to survive when run alone. The runner also exited with code 1, which Bob explained away instead of fixing | Runner rewritten: baseline is run 3 times in parallel to collect `flakyTests`; a mutant is `caught` only if its failing tests minus `flakyTests` are non-empty; otherwise status is `survived` with `onlyFlakyFailures: true`. Windows `shell: true` (DEP0190) replaced with `npx.cmd` direct invocation to eliminate the spurious non-zero exit | `b9af7c1` |
| 5 | 2026-09-25 | Task 06, Guardian mode | A class-level size() test for FP-10 | **The test could never fail.** size() returns Math.min(count, maxSize); with maxSize 2 a double count is clamped to the same answer. Bob then replaced it with an easier iteration test and removed the only size() test | Review asked for a real size() test. Bob found a reachable state with a partial old cache (delete), where the double count shows: 2 vs 3. FP-10-H caught | `b09189b` |
| 6 | 2026-09-25 | Tasks 07 and 09, Saboteur mode | Mutants labelled "semantic" | 4 mutants only negated a condition or flipped an operator, the same thing a classic mutation tool generates | Relabelled classic-operator after review, twice. A rule in the prompt did not become a habit | `f5298a2`, `eb00564` |
| 7 | 2026-09-25 | Task 07, runner | Validation rejected a round | **The human-written rule was wrong.** "No two mutants share a find string" rejected two real, different bugs on the same line | Rule replaced: duplicates are mutants that produce identical source. Verified with a planted duplicate of FP-08 using a longer find string: rejected | `f5298a2` |
| 8 | 2026-09-25 | Task 10, Agent mode | Scout ranked unflatten at 19 test mentions | The human baseline said 25. **The human count was wrong:** it matched the substring inside the variable name unflattened (6 lines). Bob matched whole words | Bob result kept. Checking has to run in both directions | `edfed78` |
| 9 | 2026-09-25 | Task 13, parallel subagents | Two pull request drafts and a usage statement "within word limits" | The quick-lru draft said 10 + 11 = 21 tests; the files hold 11 + 13 = 24. **The dot-prop draft described a "1abc" test case that does not exist**: the human prompt mentioned the case as background and the subagent wrote it up as covered. The usage statement miscounted mutants and task numbers | Corrected by review against the files on disk. Bobcoins were exhausted, so the missing test was not added; the pull request now says the case is not covered | `6a527d6` |
| 10 | 2026-09-25 | Tasks 04 and 11, runner and Saboteur | dot-prop scout round: DP-R2-2 caught, 2 / 3 then 3 / 3 | **Found by a cold review (Codex, OpenAI) acting as a judge.** DP-R2-2 was "caught" only by `SyntaxError: missing ) after argument list`. Root cause in the runner: `String.replace` expanded `$&` in the bug's replacement text and pasted the whole line into itself. The same review found every dot-prop result recorded quick-lru's commit `a2190eb` | Claude changed the runner: the replacement is now inserted literally, the validator rejects any bug whose edited source does not parse (checked with a planted broken bug), and each subject records its own commit. Rerun: DP-R2-2 is caught by 7 real tests; statuses unchanged. DP-R2-2's description does not match its edit; a review note says so | the commit that adds this row |

The check script is the same one the runner uses before any mutation run.
